# Topology + services YAML carrying the faucet seed and alloy credentials
network/ansible.yml
# xrpld-builder work tree and xrpld-lab cluster keystore
workspace/
# IMAGE/BUILD_SERVER/BUILD_VERSION written by `make build`
.last-build*.env
.venv
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
# Per-checkout WORKSPACE/ANSIBLE_CONFIG overrides
.env.mk
